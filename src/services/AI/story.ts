// services/storyService.ts

export const processStoryService = async (storyText: string) => {
  if (!storyText || storyText.trim() === "") {
    throw new Error("Story text is required");
  }

  
  
};

export const autoSubtaskGenService = async (storyText: string) => {
  if (!storyText || storyText.trim() === "") {
    throw new Error("Story text is required");
  }

  
 


};
